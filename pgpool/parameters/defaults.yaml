# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: pgpool
  rootgroup: root
  config: '/etc/pgpool'
  service:
    name: pgpool
  subcomponent:
    config: '/etc/pgpool-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
